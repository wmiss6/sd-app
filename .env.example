# Copy this file to .env and fill in your credentials
# IMPORTANT: Never commit your actual .env file to the repository

# Forge credentials - these are directly used by Forge CLI
FORGE_DEV_EMAIL=atlassian_login
FORGE_DEV_API_TOKEN=api_key

# Keep the old variables for backward compatibility
FORGE_EMAIL=atlassian_login
FORGE_API_TOKEN=api_key

# Add any other environment variables your project needs below